# VB-OS Cloud
VBOS_URL=http://localhost:8000
# Needs: evaluate, replay, read_evaluations, read/write/approve boundaries,
# manage/approve deployments, connector_gather, read/write connectors,
# read/write flows, manage_alerts, manage_certifications, read_projects
VBOS_API_KEY=
VBOS_PROJECT_ID=
VBOS_ENVIRONMENT_ID=

# Reference PACS (Orthanc)
# Host-side URL, used by seed_studies.py
ORTHANC_URL=http://localhost:8042
# Docker-network URL, used by the VB-OS connector
ORTHANC_DICOMWEB_URL=http://accessium-orthanc:8042/dicom-web
ORTHANC_USER=vbos
ORTHANC_PASSWORD=accessium_dev

# Referring physician portal
VBOS_INGEST_CONNECTOR_ID=
GATEWAY_URL=http://host.docker.internal:9092
WEBHOOK_SIGNING_SECRET=accessium_webhook_secret_dev
INGEST_SIGNING_SECRET=accessium_ingest_secret_dev
# Set to make the portal accept fewer instances than authorized (E3 DEFER)
DELIVERY_DRIFT=0
